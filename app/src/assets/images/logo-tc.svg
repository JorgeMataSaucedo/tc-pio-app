<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" width="200" height="200">
  <defs>
    <!-- Gradiente rojo institucional -->
    <linearGradient id="tcGradient" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#D32F2F"/>
      <stop offset="100%" stop-color="#B71C1C"/>
    </linearGradient>
    <!-- Sombra -->
    <filter id="shadow" x="-20%" y="-20%" width="140%" height="140%">
      <feDropShadow dx="0" dy="2" stdDeviation="3" flood-color="#000000" flood-opacity="0.2"/>
    </filter>
  </defs>
  
  <!-- C�rculo de fondo -->
  <circle cx="100" cy="100" r="95" fill="url(#tcGradient)" filter="url(#shadow)"/>
  
  <!-- Borde interior decorativo -->
  <circle cx="100" cy="100" r="85" fill="none" stroke="rgba(255,255,255,0.2)" stroke-width="2"/>
  
  <!-- Tractocami�n estilizado (silueta simplificada) -->
  <g fill="#FFFFFF" opacity="0.15">
    <!-- Cabina -->
    <path d="M45 130 L45 95 Q45 85 55 85 L90 85 L90 130 Z"/>
    <!-- Remolque -->
    <rect x="90" y="90" width="70" height="40" rx="3"/>
    <!-- Ruedas -->
    <circle cx="60" cy="135" r="10"/>
    <circle cx="115" cy="135" r="10"/>
    <circle cx="145" cy="135" r="10"/>
  </g>
  
  <!-- Letras TC prominentes -->
  <text x="100" y="95" font-family="'Segoe UI', Arial, sans-serif" font-size="72" font-weight="800" fill="#FFFFFF" text-anchor="middle" letter-spacing="-2">TC</text>
  
  <!-- L�nea decorativa -->
  <rect x="55" y="108" width="90" height="3" rx="1.5" fill="rgba(255,255,255,0.5)"/>
  
  <!-- Texto empresa -->
  <text x="100" y="130" font-family="'Segoe UI', Arial, sans-serif" font-size="11" font-weight="600" fill="#FFFFFF" text-anchor="middle" letter-spacing="2">TRANSPORTES</text>
  <text x="100" y="145" font-family="'Segoe UI', Arial, sans-serif" font-size="11" font-weight="600" fill="#FFFFFF" text-anchor="middle" letter-spacing="2">CUAUHT�MOC</text>
</svg>
